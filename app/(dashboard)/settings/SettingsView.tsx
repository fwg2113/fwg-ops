$(cat "app/(dashboard)/settings/SettingsView.tsx" | sed 's/const togglePhoneEnabled = async (id: string, enabled: boolean) => {/const togglePhoneEnabled = async (id: string, enabled: boolean) => {\n    console.log("Toggling", id, "to", enabled)/')
